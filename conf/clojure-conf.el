;; Load and configure SLIME
;; (add-to-list 'load-path "~/soft/clojure/emacs/slime")
;; (require 'slime)
;; (eval-after-load 'slime '(setq slime-protocol-version 'ignore))
;; (slime-setup '(slime-repl))
 
;; Load a major mode for editing Clojure code.
;; (add-to-list 'load-path "~/soft/clojure/emacs/clojure-mode")
;; (require 'clojure-mode)
;; (require 'clojure-test-mode) ;; requires slime
